/**
 * Transitional Sales request barrel.
 *
 * Canonical outbound Sales DTOs live under `src/types/requests`.
 */

export type {
  CreateSaleItemRequest,
  CreateSalePaymentRequest,
  CreateSaleRequest,
  RefundSaleItemRequest,
  RefundSaleRequest,
  UpdateSaleRequest,
} from "@/types/requests";
