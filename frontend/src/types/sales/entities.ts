/**
 * Transitional Sales entity barrel.
 *
 * Canonical reusable Sales entities live under `src/types/entities`.
 */

export type {
  Sale,
  SaleItem,
  SalePayment,
  SaleRefund,
} from "@/types/entities";
