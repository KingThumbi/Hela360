/**
 * ============================================================================
 * Hela360 Base Service
 * ============================================================================
 *
 * Public exports for the shared service foundation.
 */

export { BaseService } from "./BaseService";
export { default } from "./BaseService";

export type {
  EntityIdentifier,
} from "./types";

export type {
  QueryValue,
  FilterValue,
  FilterOptions,
  PaginationOptions,
  SearchOptions,
  SortDirection,
  SortKey,
  SortOptions,
  QueryOptions,
  IdentifierCollection,
} from "./query";

export type {
  RequestOptions,
} from "./request";