/**
 * Transitional Sales runtime-value barrel.
 *
 * Canonical Sales runtime values live under `src/types/enums`.
 */

export {
  SALE_STATUSES,
} from "@/types/enums";
export type {
  PaymentMethodCode,
  SaleStatus,
} from "@/types/enums";
